{
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm build",
  "crons": [
    {
      "path": "/api/exchange-rate/update",
      "schedule": "0 11 * * *"
    }
  ]
}
