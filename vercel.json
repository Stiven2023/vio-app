{
  "installCommand": "npm i -g pnpm@9.15.9 && pnpm -v && pnpm install --frozen-lockfile",
  "buildCommand": "npm i -g pnpm@9.15.9 && pnpm -v && pnpm build",
  "crons": [
    {
      "path": "/api/exchange-rate/update",
      "schedule": "0 11 * * *"
    }
  ]
}
